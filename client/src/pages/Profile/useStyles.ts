import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    backgroundColor: '#fff',
    width: '100%',
    height: '100%',
  },
}));

export default useStyles;
